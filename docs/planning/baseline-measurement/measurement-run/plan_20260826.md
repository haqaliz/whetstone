# Plan — measurement-run (2026-08-26)

Aspect 4 of `baseline-measurement` (PRD: `../prd.md`, spec: `./spec.md`). Strict TDD;
branch `feat/baseline-measurement/aliz` in
`/Users/aliz/dev/at/whetstone/.claude/worktrees/feat-baseline-measurement`. Aspects 1–3 landed
(the untrained checkpoint, the door, the committed artifact home).

## Context (already dug)

- `tests/test_gate_runbook_guards.py` — the closest sibling: imports `_bash_blocks`,
  `_named_paths`, `_worktree_name` from `test_runbook_guards.py` **by identity**; pins
  RUNBOOK path, DOORS, WORKTREE, STALE_WORKTREES, nine properties; watched failing against a
  deliberately wrong stub sheet first. READ IT FULLY before planning the guard.
- `tests/test_runbook_guards.py` — the shared parse helpers (imported by identity, never
  re-implemented).
- `src/whetstone/loop/baseline.py` — `build_parser()` (the module door's flag surface — the
  sheet's flags must exist there), `main`, `measure`, `render_artifact`, `BASELINE_SCHEMA`,
  `EVIDENCE_SCHEMA`.
- The night-runbook precedent for the candidate-resolution block:
  `docs/planning/p2-rollouts/night-door/runbook.md` (the 32B resolution).
- The gate-runbook precedent for the measured-once/killed-run wording:
  `docs/planning/p3-promotion-gate/gate-runbook/runbook.md`.

## File-by-file build order

1. **`tests/test_baseline_runbook_guards.py`** (new) — the guard, written against a
   deliberately wrong stub sheet first (RED), then the real sheet (GREEN).
2. **`docs/planning/baseline-measurement/measurement-run/runbook.md`** (new) — the real
   sheet.
3. **`docs/planning/baseline-measurement/measurement-run/{spec.md,plan_20260826.md}`** —
   write-up commit.

## Tasks

### Task 1 — the guard, watched failing against a wrong stub sheet

**Files:** `tests/test_baseline_runbook_guards.py`, a stub `runbook.md` (deliberately wrong,
staged at the real path so the guard reads it).

1. Write the stub sheet FIRST: relative writable paths, a `--retries`-style flag the parser
   does not define, a stale worktree name, a "re-measure to confirm the number" instruction,
   no § 7.3 sentence, no fixture-verification block, no post-run chain.
2. RED: write the guard tests (below) against the stub — every property test must FAIL
   against the stub (watched failing first; record which tests failed and why).
3. GREEN comes in Task 2 with the real sheet; Task 1's commit is the guard + the stub? NO —
   the repo precedent (gate-runbook) landed the guard and the real sheet together
   ("Write the gate runbook and its guard green"). So Task 1 is the RED half only, and the
   commit lands after Task 2. Keep the stub uncommitted or in a temp name.

The guard's properties (import the shared parse helpers by identity, asserted `is`):

1. `test_the_parse_really_reads_the_sheet` (anti-vacuity: at least one command block parsed).
2. `test_every_flag_the_chain_passes_exists_in_the_door_parser` — flags from every `python -m
   whetstone.loop.baseline` block against `baseline.build_parser()` (import
   `whetstone.loop.baseline` — the module door, NOT `whetstone.cli`).
3. `test_every_writable_path_is_absolute` — `--runs`, `--workspace`, `--out`, `--checkpoint`,
   `--weights`, `--tasks`, `--public`, `--heldout`, `--pool` values are absolute.
4. `test_exactly_one_worktree_and_no_stale_one` — the sheet names the unit's worktree
   (`feat-baseline-measurement`) exactly once and no stale name from `STALE_WORKTREES`
   (the gate guard's list, reused).
5. `test_the_measured_once_discipline_is_a_refusal_not_a_rerun` — the sheet contains
   "refus" wording for the same-series case and NOWHERE tells the operator to re-measure /
   re-render / "confirm" the number (the roadmap's own response if the gate cannot fire is
   the only rerun-adjacent sentence allowed: none here — this is a measurement, not a gate).
6. `test_the_machinery_is_verified_before_the_real_pass` — the sheet's before-block names
   the fixture suites (`tests/loop/test_baseline_door.py` etc.) and requires them green.
7. `test_the_candidate_resolution_names_the_32b_and_keeps_section_7_3_open` — the 32B
   repo_id present, "§ 7.3 stays open" (or equivalent "§ 7.3 ... open" phrasing) present,
   and neither "pinned base" nor "selected base" appears.
8. `test_the_outcomes_are_publishable_never_a_halt` — "zero" and "coverage" stated as
   publishable (a coverage below 12 of 12 is disclosed, never a halt).
9. `test_the_killed_run_behavior_and_recorded_on_are_stated` — a fresh `--run-id` on a
   killed run, a half-written artifact refused by schema, and `--recorded-on` stated as an
   input.
10. `test_the_post_run_chain_is_present` — the render command (`--render <evidence>` with
    `--out reports/baseline-measurement`) and the finding step named.

### Task 2 — the real sheet (GREEN) + write-up

**Files:** `docs/planning/baseline-measurement/measurement-run/runbook.md` (real),
`tests/test_baseline_runbook_guards.py` (final), the spec + plan.

1. Write the real sheet on the gate-runbook structure: candidate resolution (the 32B, § 7.3
   open), the before-you-run block (mlx extra, the fixture verification command, the
   untrained-checkpoint materialization step — a `python -c`/module invocation of the
   aspect-1 writer or a named helper, its flags pinned if it is a command), the arm command
   (absolute paths, `--recorded-on`, `--run-id`), the render chain, the halt conditions
   (same-series refusal), the killed-run restart, the post-run chain (evidence review →
   render → finding), the outcomes (zero/coverage publishable).
2. GREEN: run the guard — all ten properties pass against the real sheet.
3. Validation: `uv run pytest tests/test_baseline_runbook_guards.py -q` green;
   `uv run pytest tests/loop/test_baseline_door.py tests/loop/test_baseline_document.py
   tests/bakeoff/test_baseline_report.py -q` green (no production code changed — the whole
   aspect is the guard + the sheet); ruff + mypy clean.
4. Commit 1 (guard + real sheet + stub deleted): `Write the baseline runbook and its guard
   green`. Commit 2 (write-up): `Write up the measurement-run aspect`.

## Edge cases & error handling

- The stub sheet must be deleted/replaced before the commit — a stub in the tree is a
  failing guard.
- The render command's flags must exist in `baseline.build_parser` (they do — `--render`,
  `--evidence`, `--out`, `--recorded-on`, `--checkpoint` — the guard asserts them).
- The sheet must not restate a figure about a model (the finding's numbers belong to the
  operator's post-run commit, and the sheet is committed before the run).

## Agent execution notes

- One agent owns Tasks 1–2 (the stub is the RED half of the same unit). Checkpoint at the
  stub-REFUSED state (all ten failing against the stub) before writing the real sheet.
- Import the shared parse helpers and the stale-worktree list by identity; do not mutate
  another guard's globals.
- If the parser lacks a flag the sheet needs (e.g. a materialize-checkpoint command), STOP
  and report — adding a flag to `baseline.build_parser` is a production change that belongs
  to aspect 2/3, not this aspect's silent scope.