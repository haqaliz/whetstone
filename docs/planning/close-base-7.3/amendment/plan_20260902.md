# Implementation plan — close-base-7.3 / amendment

**Plan:** 2026-09-02 · **Executes:** `docs/planning/close-base-7.3/prd.md` + `amendment/spec.md`
**Branch:** `feat/close-base-7.3/aliz` · **Worktree:** `.claude/worktrees/feat-close-base-7.3`

## Project setup checklist

- `uv sync` in the worktree first — no venv exists there yet (`.venv` is per-worktree, not
  shared). No `--extra mlx` needed: the suite injects stub engines; nothing here calls a model.
- No new dependencies. No env vars. No network use.
- Files touched in this unit: `PREREGISTRATION.md`, `docs/ROADMAP.md`, `CLAUDE.md`,
  `docs/STATUS.md`, `CHANGELOG.md`, `tests/test_docs.py`. **No file under `src/`.**

## Pinned facts (verified 2026-09-02; do not re-derive)

- repo_id: `mlx-community/Qwen2.5-Coder-32B-Instruct-4bit` — byte-identical in
  `tests/test_night_runbook_guards.py:65` (`RETAINED`), night runbook `:14,57,92`,
  `weights/provenance.json:497`, `finding.md:13`.
- revision: `d1e3b690c8e225d7795bccddf971ca6be68b2012` — `weights/provenance.json:498`,
  `finding.md:22`. Gitignored; absent from CI; operator-verified only.
- Cross-test imports are the house pattern (`tests/test_night_runbook_guards.py:41`,
  `tests/test_baseline_runbook_guards.py:49`): `from test_night_runbook_guards import RETAINED`
  works under pytest's prepend mode (`testpaths = ["tests"]`, no `tests/__init__.py`).
- `tests/test_docs.py` has `_read` and `_flat` helpers; the § 10.9 shape guard at
  `:726-761` is the model for the new guard.
- `ROADMAP_CITATIONS` tuple at `tests/test_docs.py:137-148` maps PREREGISTRATION's
  `docs/ROADMAP.md:NN-NN` citations to anchored ranges — inserting lines into ROADMAP below
  `:375` shifts later anchors and may break `test_every_roadmap_citation_in_the_preregistration_resolves`.

## Phase 1 — RED: the shape guard

- **Goal:** a new test in `tests/test_docs.py` that fails against today's
  `PREREGISTRATION.md` for exactly the intended reasons (no § 10.10, no log row, no pin).
- **Prerequisites:** `uv sync` done.
- **Files:** `tests/test_docs.py` (add one test after the § 10.9 guard at `:761`).
- **Test, in the § 10.9 shape** (`test_the_honest_number_amendment_claims_no_count_measured_here`
  as the model — docstring + per-assertion refusal messages; helper imports
  `from test_night_runbook_guards import RETAINED`):

```python
def test_the_base_amendment_closes_section_7_3_and_claims_no_count_measured_here() -> None:
    flat = _flat(_read(PREREGISTRATION))
    assert "10.10" in flat, ...            # § 10.10 exists
    assert "Type 1 (§ 8.1): closes § 7.3" in flat, ...   # Type 1 closure of § 7.3
    assert RETAINED in flat, ...           # the pinned base is named (cross-pin)
    assert "At the time of this amendment no night has run, no count has been " \
           "measured under it, and none is claimed here" in flat, ...  # no-measurement claim
    assert "§ 7.3 is closed by the amendment below (§ 10.10)" in flat, ...  # status edit
    assert "The fine-tuned base is pinned; § 7.3 is closed by the amendment below (§ 10.10)" in flat, ...  # the § 10.10 log row (corrected 2026-09-02: the generic "1 — closes an open item" + "| Yes |" pair passed vacuously against the § 10.7/§ 10.8 rows — the log-row pin must name the row's own text)
```

- **Validation:** `uv run pytest tests/test_docs.py -k base_amendment` fails (RED), each
  assertion failing for its intended reason; `uv run pytest tests/test_docs.py` — the rest
  of the file stays green.
- **Commit:** `Pin the § 7.3 closure amendment's shape (TDD, RED)`

## Phase 2 — GREEN: the amendment

- **Goal:** `PREREGISTRATION.md` satisfies the guard.
- **Prerequisites:** Phase 1 committed.
- **Files:** `PREREGISTRATION.md` — three edits only:
  1. **Status paragraph** (`:321-324`): replace the three § 7.3 sentences with the closure
     sentence. From "§ 7.3 is still **open** — in particular the P1 bake-off selected no
     base, so it does **not** close § 7.3, and an amendment that tried to close it after
     the fact would document only that the item was never closed (§ 8.1)." to "§ 7.3 is
     closed by the dated amendment below (§ 10.10)." Nothing else in the paragraph (or
     anywhere above § 10) changes.
  2. **Amendment log row** appended after `:318`:
     `| 2026-09-02 | The fine-tuned base is pinned; § 7.3 is closed by the amendment below (§ 10.10) | 1 — closes an open item | Yes |`
  3. **§ 10.10** appended after § 10.9, using **this exact text** (drafted and pinned; do
     not free-write a pre-registration):

```markdown
### 10.10 The fine-tuned base is pinned, closing § 7.3 — 2026-09-02

**Type 1 (§ 8.1): closes § 7.3, committed before the night it governs runs.** It introduces
no success threshold and rewords nothing in § 1, § 4, or § 6.

**The base.** The nightly loop fine-tunes `mlx-community/Qwen2.5-Coder-32B-Instruct-4bit`,
pinned at the immutable revision `d1e3b690c8e225d7795bccddf971ca6be68b2012` and recorded by
per-file hash in `weights/provenance.json` (gitignored — the weights never leave the box),
re-hashed on every run by the harness. The night runbook's resolution block names the same
base (`docs/planning/p2-rollouts/night-door/runbook.md`), and the run ledger records it, so
the base this amendment pins and the base the night runs are one and the same input.

**The evidence it was chosen on.** § 7.3 asks that the base be decided on evidence against
the working verifier, not on paper. The larger-base arm is that evidence: it scored this
candidate on the declared source-B set under the hardened contract with the control
discipline intact, and it produced the first nonzero strict-PASS yield the harness has ever
measured. The arm's figures have one home, `reports/larger-base/`, and this amendment
restates none of them; the arm's finding names the fork decision and the closure rule this
amendment performs (`docs/planning/larger-base-arm/finding.md`). The base is named as the
best available evidence, not as a selection the evidence exceeds.

**What is not claimed.** No night has run: no training set, checkpoint or yield figure
exists. At the time of this amendment no night has run, no count has been measured under it,
and none is claimed here — this amendment precedes night #1, which is the whole of its
value, and the pinned baseline measurement of § 3 remains unspent. The base is pinned for
this series; it remains deliberately swappable, and a change of base is a further Type 1
amendment committed before the measurement it governs runs (§ 8.1).
```

- **Constraints check (all must hold):** no `%`/`percent`/`percentage`; no placeholder
  tokens; no digit that describes a model (the "first nonzero" claim carries none); the
  phrase "no figure about a model" in § 4's area untouched; no `docs/ROADMAP.md:NN-NN`
  citation added (the text cites file paths only).
- **Validation:** `uv run pytest tests/test_docs.py -k base_amendment` passes (GREEN);
  `uv run pytest tests/test_docs.py` fully green; `git diff` on `PREREGISTRATION.md` shows
  exactly the three edits.
- **Commit:** `Close § 7.3 by a Type 1 amendment: the fine-tuned base is pinned before night #1 (TDD)`

## Phase 3 — the state files

- **Goal:** the repo's current-state files describe the tree this unit ships.
- **Prerequisites:** Phase 2 committed.
- **Files and exact edits:**
  1. **`docs/ROADMAP.md`** — after the "What the bake-off found" paragraph (ends at
     `:375`, "...`reports/baseline/` alone."), insert a dated correction blockquote on the
     `:364-368` precedent:

     ```markdown
     > **Corrected 2026-09-02, when the § 7.3 Type 1 amendment landed.** This paragraph
     > asserted that no base was selected and that `PREREGISTRATION.md` § 7.3 stays open.
     > Both were exact until the amendment and are false now; the sentences are preserved
     > as quotations rather than deleted, because they describe what the bake-off found.
     > The bake-off's zero remains the dated record; the larger-base arm's evidence — the
     > first nonzero strict-PASS yield, control intact — became the basis, and § 7.3 is
     > closed by `PREREGISTRATION.md` § 10.10 naming
     > `mlx-community/Qwen2.5-Coder-32B-Instruct-4bit` as the fine-tuned base, before night
     > #1 trains.
     ```
  2. **`CLAUDE.md`** — (a) the top status block's "Most recently" chain gains: "Then the
     **close-base-7.3** amendment (2026-09-02): `PREREGISTRATION.md` § 7.3 is closed by the
     Type 1 amendment (§ 10.10), naming `mlx-community/Qwen2.5-Coder-32B-Instruct-4bit` as
     the base the night fine-tunes, committed before night #1 trains." (b) the "Still open"
     bullet at `:151-153` is replaced by a settled bullet: "**Which open base we fine-tune /
     LoRA — closed 2026-09-02 by the § 7.3 Type 1 amendment** (`PREREGISTRATION.md` § 10.10):
     `mlx-community/Qwen2.5-Coder-32B-Instruct-4bit`, chosen on the larger-base arm's first
     nonzero strict-PASS yield." Nothing else in CLAUDE.md changes.
  3. **`docs/STATUS.md`** — a top entry (newest first), in the house voice, recording: the
     unit, the amendment's content (§ 10.10, log row, status edit), the shape guard (RED
     first, cross-pin on `RETAINED`), the ROADMAP/CLAUDE updates, and the honest tail: the
     night has still not been run, and the gate has still not run on real checkpoints.
  4. **`CHANGELOG.md`** — new `## [0.13.0] - 2026-09-02` section above `[0.12.0]`, per the
     `213b6c6` precedent (the unit's own commit creates the heading; the cut comes at
     merge): an `### Added` entry for the amendment + the shape guard, and a `### Not
     claimed` block: the night has not been run and nothing is published (no new series, no
     § 10 amendment beyond this unit's own).
- **Validation:** `uv run pytest tests/test_docs.py` — **watch for the shifted-anchor
  hazard**: if `test_every_roadmap_citation_in_the_preregistration_resolves` fails on a
  range below the insertion point, extend `ROADMAP_CITATIONS` (test_docs.py:137-148) in
  the same commit — the tuple is the single place mapping PREREGISTRATION citations to
  ranges; the test names the offender. Full suite: `uv run pytest` green.
- **Commit:** `Record the close-base-7.3 unit in the roadmap, status log, changelog and CLAUDE.md`

## Phase 4 — full gate + operator verification

- **Goal:** the unit is green and the pin is machine-true.
- **Prerequisites:** Phases 1-3 committed.
- **Steps (integrator, not an agent):**
  1. `uv run pytest` (full), `uv run ruff check .`, `uv run mypy src/` — all green.
  2. `git log --oneline` shows the three unit commits, RED first.
  3. **Operator verification (AC7 — cannot be automated, provenance is gitignored):** in
     the PRIMARY checkout, confirm `weights/provenance.json:497-498` carries the repo_id
     and revision named in § 10.10, and spot-check a per-file hash matches `finding.md`'s
     record. Report the confirmation in the PR body.
- **Commit:** none (verification only).

## Edge cases and error states

- **Guard can't be RED for its intended reasons** — e.g. it passes vacuously because a
  pinned string already exists elsewhere in the doc: run `-k base_amendment` before Phase 2
  and confirm each assertion fails for its stated reason; if one passes, the pin is wrong,
  fix the test, not the doc. **Happened at Phase 1**: the log-row pin
  (`"1 — closes an open item"` + `"| Yes |"`) passed vacuously against the § 10.7/§ 10.8
  rows and was corrected to pin the § 10.10 row's own text.
- **Shifted ROADMAP anchors** (Phase 3): handled above — extend `ROADMAP_CITATIONS`, never
  reword PREREGISTRATION's citations.
- **`%`-guard trip** (e.g. "100%" nowhere — the drafted text contains none): if a commit
  fails `test_the_preregistration_states_no_figure_about_a_model`, remove the token from
  the edit; never touch the guard.
- **No UNVERIFIED path** — nothing in this unit evaluates; the honesty contract binds the
  *words*: no count appears that a verifier didn't produce.

## Agent execution notes

- Phase 1 and 2 must not be merged into one commit — the RED commit is the unit's proof of
  test-first.
- The § 10.10 text is pinned verbatim in this plan; agents must copy it, not paraphrase.
- Human checkpoint: after Phase 2, before Phase 3 (the state files touch the repo's
  front-door docs).
- The final PR body must state the operator verification result and the honest tail
  (night not run, gate not run on real checkpoints).