# Executed sequence — `local-home` (2026-08-28)

**Recorded after the fact, and labelled as such.** Aspect 1 got a forward plan
(`../night-evidence/plan_20260828.md`). Aspects 2–4 were executed directly from their specs,
whose acceptance criteria were already test-shaped, and back-dating a "plan" onto work that is
already done would be a fiction in a repository whose whole subject is not fooling yourself. This
records what actually happened, so the artifact layout is complete and honest at the same time.

## What was built

`morning.refuse_published_out` + `LOCAL`, in `src/whetstone/loop/morning.py`;
`tests/loop/test_morning_home.py`.

## Sequence

1. **RED** — 11 tests written first; 8 failed, 3 passed (the three that assert facts about the
   repository rather than about new code: the `.gitignore` rule, its anti-vacuity control, and
   the one-home guard's carve-out).
2. **GREEN** — the narrower sibling of `night._refuse_published_root`, reusing `PUBLISHED` and
   `TranscriptNotPrivate` by identity and checking the **resolved** path.
3. **Watched failing, both directions** — a credulous predicate that accepts everything loses 3
   tests; a paranoid one with no carve-out loses the 2 controls. Neither mutation is caught by
   the other's tests, which is what makes both worth their lines.

## What the build changed about the plan

Nothing in this aspect. The collision it was written to resolve — `_refuse_published_root`
refusing this unit's own home — was already found in the dig and is why the aspect exists.

## Carried out of it

The correction to aspect 1's no-inference guard (see `../night-evidence/`): it banned first-party
imports by name suffix, a rule invented here rather than the tree's, and it had been passing
vacuously since aspect 1. Replaced with a direct ban plus a cold-start measurement.
