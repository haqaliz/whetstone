# Plan — amendment-and-home · 2026-08-27

Plans aspect 4 of `docs/planning/honest-number-report/prd.md` (P4 slice 2). Executed in Phase 6
by the agents team under strict TDD (RED → GREEN → REFACTOR; suite green after each task;
commit per task on `feat/honest-number-report/aliz`).

## Project setup

No new dependencies. Read before writing: `PREREGISTRATION.md` § 10.6 (the larger-base
amendment, lines ~469-498 — the Type 2 shape to mirror) and § 8 (the amendment rule);
`tests/test_docs.py` (the no-figure/placeholder/co-existence guards);
`tests/bakeoff/test_report.py` lines 1322-1327 (`_EXISTING_HOMES`/`_ALL_HOMES`), 1340-1447
(disjointness + planted controls), 1875-1986 (the 15-artifact list + docstring history +
planted-artifact control), and `tests/bakeoff/test_transcript_locality.py:73-160` (the
opposite-sign twin).

## Phase 1: RED — the guard's sixth move, tested before the list changes

- **Goal:** prove the current guard fails against the new home's presence (the RED is the
  un-amended guard), then extend it.
- **Files:** `tests/bakeoff/test_report.py` and `tests/bakeoff/test_transcript_locality.py`.
- **Tests:**
  1. plant `reports/honest-number/report.md` in `tmp_path` and assert the existing scan
     flags it (proves the mechanism sees the new directory — RED against today's list is
     expected at this point: the planted-tree test rebuilds the 15-artifact state, so it
     passes; the RED comes from the list-extension test below);
  2. the new list-extension test: the authoritative scan over the real tree must find
     exactly 18 artifacts (six homes × three) **once the committed artifacts exist** — write
     this test to assert `honest-number` in the enumeration with the docstring argument
     (watched failing: today's list is 15 and the committed artifacts do not exist yet).
- **Validation:** the extension test fails for the right reasons (list 15 ≠ 18, missing
  artifacts), not for a broken scan.

## Phase 2: GREEN — the § 10.9 amendment and the committed declaration-only home

- **Files to modify:** `PREREGISTRATION.md` (§ 10.9, amendment log row, dated 2026-08-27);
  create `reports/honest-number/{report.md,report.json,cost.json}` via the writer's
  declaration state (aspect 2) — **generated by the door's `--render-declaration`, never
  hand-typed**; extend the guard lists in both test files (18 artifacts) with the argument
  restated in both docstrings; extend `_ALL_HOMES`.
- **The amendment's content (mirror § 10.6's shape):**
  - declares the final side's generation contract: the loop's seeded categorical sampler
    (`sampling.K = 8`), differing from every published contract's greedy sampler — recorded
    in every run ledger, disclosed here before any figure;
  - declares `reports/honest-number/` the only home of the delta/final series' figures;
  - names the existing homes non-comparable (the § 10.6 sentence shape);
  - states the baseline exception: the report renders the § 3 baseline's figures through the
    sealed artifact (the § 4 shape requires it) — the exception is loader-by-identity only;
  - the "At the time of this amendment no count has been measured under the report, and none
    is claimed here" sentence;
  - no success threshold; nothing above § 10 edited; no proportion in any spelling.
- **Validation:** Phase 1 green; `uv run pytest tests/test_docs.py` green;
  `uv run pytest tests/bakeoff/test_report.py tests/bakeoff/test_transcript_locality.py` green;
  regeneration of the committed artifacts is byte-identical (run the door's
  `--render-declaration` into a temp dir and diff).
- **Commit:** `Disclose the honest-number series (§ 10.9) and commit its declaration-only home (TDD)`

## Phase 3: RED+GREEN — the disjointness scans and the planted controls

**Addition found during aspect 2 (2026-08-27): the funnel-figures exception.** Source A's
funnel counts (`1 of 300`, `299 of 300`, per-gate refusals) are part of the § 4 shape
(`PREREGISTRATION.md:140-155` renders the filter's funnel beside the instance) AND part of the
committed `reports/baseline/` artifacts. The honest-number report must render them, so the
disjointness scan admits a **second, ledger-derived exception**: committed corpus facts (the
four-gate funnel over SWE-bench-Lite's 300, from `tasks/public/ineligible.json`'s
denominators) that cannot disagree with themselves — asserted equal to the corpus ledger,
never recomputed, exactly as the baseline-loader exception is asserted byte-equal to the
sealed artifact. Both exceptions are named in the § 10.9 disclosure and in the guard
docstrings' sixth-move argument.

- **Goal:** the new home is scanned like every other; a planted figure or artifact still
  fails.
- **Tests (RED first):**
  - `_ALL_HOMES`-driven `_committed_figures` scan now includes `honest-number` (a planted
    `10 of 20` in a copy of the new home's artifacts is flagged);
  - the writer's disjointness test (aspect 2) is re-run against the now-committed home
    (the writer's output is disjoint from the four existing homes, with the loader-derived
    baseline exception asserted — aspect 2's Phase 4 test already covers the writer side; this
    phase proves the home side);
  - the planted-artifact control still catches a sixth planted artifact.
- **Files:** extend `tests/bakeoff/test_report.py` (the `_ALL_HOMES` machinery and the
  planted controls).
- **Validation:** green; each control watched failing against the pre-change state first
  where the test's intent is new.
- **Commit:** `Scan the honest-number home in the disjointness guards (TDD)`

## Phase 4: integration pass

- **Goal:** the whole tree agrees with the new home.
- **Steps:** full `uv run pytest`; `uv run ruff check . && uv run mypy src/`; verify the
  committed artifacts hold no `\d+ of \d+` (the declaration tests in aspect 2's file);
  `git diff --stat` shows `PREREGISTRATION.md`, the two guard test files, and the three new
  artifacts only.
- **Commit:** fold into Phase 3 unless cleanup is needed.

## Edge cases (from the spec)

- The amendment must not trip `tests/test_docs.py`'s no-figure guard: no `%`, no
  `percent`, no `percentage` in any spelling, no placeholder tokens.
- The committed artifacts are writer-generated: a hand-typed twin (even byte-equal text) is
  refused by the declaration test (the writer's constant is the provenance).
- The two guard copies move in lock-step; a one-sided edit fails the other test — that is
  the discipline, not an annoyance.

## Agent execution notes

- Do not invent the amendment's wording — mirror § 10.6's sentences (the discipline is
  precedent-shaped) and keep every claim citable.
- The § 7.3 Type 1 amendment is explicitly NOT this aspect; do not touch § 7.
- `PREREGISTRATION.md` is append-only: nothing above § 10 changes, the log row is appended,
  the date is 2026-08-27.