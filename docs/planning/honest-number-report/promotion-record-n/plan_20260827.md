# Plan — promotion-record-n · 2026-08-27

Plans aspect 1 of `docs/planning/honest-number-report/prd.md` (P4 slice 2). Executed in Phase 6
by the agents team under strict TDD: each phase's test is written first (RED), then made green,
then refactored; the suite runs after each task; commit per task on `feat/honest-number-report/aliz`.

## Project setup

Worktree already exists and is synced (`uv sync` done). No new dependencies. No config changes.
Relevant existing files:

- `src/whetstone/loop/gate.py` — `SideCounts` (~line 1116 `_counts_payload`), `PROMOTION_SCHEMA`
  (line 100), `write_promotion_record` (741-781), `_counts` (the tally used at scoring time).
- `src/whetstone/bakeoff/report.py` — `tally` (426-453, `weaker_wins` at 444-448), `_UNCOVERED`
  (60).
- `tests/loop/test_gate_door.py` (or wherever the promotion-record tests live — locate with
  `grep -rn "whetstone-promotion" tests/`), `tests/bakeoff/test_report.py`.

## Phase 1: RED — the failing test for `weaker_wins` in `SideCounts`

- **Goal:** prove the field is missing today and define its value by identity.
- **Files:** a new `tests/loop/test_promotion_record_n.py` (or extend the existing record
  tests — prefer the new file so the aspect is self-contained).
- **Test 1 (the differential):** build a fixture set of `Rollout` records with known
  weak/strict statuses (some `weak=PASS, strict=FAIL`), run the gate's own `_counts`-equivalent
  path (the promotion-record writer's payload builder), and assert `weaker_wins` equals
  `report.tally(...).weaker_wins` **by identity** — i.e. the fixture's N is computed the
  single way `PREREGISTRATION.md` defines N. Watched failing first: the field does not exist
  in `SideCounts` today.
- **Test 2:** a round-trip record (write → `read_promotion_record`) carries every count
  verbatim.
- **Validation:** `uv run pytest tests/loop/test_promotion_record_n.py` → the tests fail for
  the field's absence, not for an assertion bug.

## Phase 2: GREEN — add `weaker_wins` to `SideCounts` and the payload

- **Files to modify:** `src/whetstone/loop/gate.py` (`SideCounts`, `_counts_payload`,
  `write_promotion_record`'s schema docstring).
- **Changes:** `SideCounts` gains `weaker_wins: int`; the scoring-time `_counts(rollouts,
  tasks)` computes it as the `report.tally` definition by identity (import `tally` or the
  exact predicate from `bakeoff.report` — reuse, never copy; assert `is` where the repo
  pattern demands); `_counts_payload` emits it; the schema docstring documents the field.
- **Validation:** Phase 1's tests green; `uv run pytest tests/loop/` green (the gate's own
  tests updated where they construct `SideCounts` — positional/field-count breaks are the
  expected cost, fix them in the same task).
- **Commit:** `Record the promotion side's weaker_wins (N_final) in the gate's payload (TDD)`

## Phase 3: RED — the fail-closed reader's refusals

- **Goal:** define `read_promotion_record` and its refusal surface before it exists.
- **Files:** extend `tests/loop/test_promotion_record_n.py`.
- **Tests, each watched failing first:**
  - unreadable file / invalid JSON → refused by name;
  - wrong/missing schema → refused;
  - unknown field → refused;
  - counts not summing (`solved + failed + unverified != denominator`) → refused;
  - `weaker_wins > denominator` → refused;
  - **a pre-field fixture (no `weaker_wins`) → refused by name, never defaulted to zero**;
  - a clean round-trip returns the counts verbatim (already in Phase 1, keep it).
- **Validation:** all fail against `src/whetstone/loop/gate.py`'s current surface (no reader
  exists → import error or missing-name failure).

## Phase 4: GREEN — implement `read_promotion_record`

- **Files to modify:** `src/whetstone/loop/gate.py`.
- **Changes:** `read_promotion_record(path) -> PromotionRecord` (frozen dataclass mirroring
  the record's fields; counts validated per the refusals above; `sides` per side per source;
  decision block verbatim). Refusals are `ValueError` subclasses or plain `ValueError` with
  messages naming the file and the offending field — follow `loop/baseline.py`'s loader
  posture (named messages, no dedicated classes beyond what the repo's pattern needs).
- **Validation:** Phase 3 green; `uv run pytest tests/loop/` green; `uv run ruff check . &&
  uv run mypy src/` green.
- **Commit:** `Read the promotion record fail-closed, never defaulting a missing count (TDD)`

## Phase 5: integration pass

- **Goal:** the aspect's whole surface holds together and nothing else moved.
- **Files:** none new.
- **Steps:** run `uv run pytest` (full suite); confirm the AC2 pins pass
  (`uv run pytest tests/ -k "ac2 or pin"` — locate the exact names via
  `grep -rn "origin/master" tests/`); confirm `tests/test_reward_path_scope_is_partitioned.py`
  passes untouched (no new edges — `gate.py` is already in the exempt `loop` package).
- **Validation:** full suite green; `git diff --stat` shows only `gate.py` + tests touched.
- **Commit:** (only if something needed cleanup) or fold into Phase 4's commit.

## Edge cases (from the spec)

- A pre-field record must be refused, never read as `weaker_wins = 0` — zero is a
  measurement, absence is a fact (spec AC 4).
- `bool` is an int subclass in Python — reject booleans for count fields exactly as
  `baseline.py`'s loader does (`_BASELINE_COUNT_FIELDS` posture, baseline.py:456-467).

## Agent execution notes

- Locate the existing promotion-record tests first (`grep -rn "whetstone-promotion" tests/`);
  the gate's own suite must stay green — updating constructors is part of the change, not a
  surprise.
- If `SideCounts` construction sites are numerous, prefer keyword args over positional in the
  fixture updates.
- No inference imports, no network, no model calls anywhere in this aspect.