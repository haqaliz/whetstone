# Plan — runbook (2026-09-05)

**Aspect:** `runbook` · **Branch:** `feat/probe-decision-gate/aliz` · **Worktree:**
`/Users/aliz/dev/at/whetstone/.claude/worktrees/feat-probe-decision-gate`
**Spec:** `docs/planning/probe-decision-gate/runbook/spec.md`

Strict TDD, code first: the guard's new assertions are written and watched failing against a
stub sheet **before** the real sheet is rewritten.

## Prerequisites

- `check-core` and `cli-door` landed: `whetstone check-probe --run …` exists in
  `build_parser()`.
- The current runbook: `docs/planning/p2-rollouts/night-door/runbook.md`, probe-pass section
  at lines 44-80, decision rule at 78-80.

## Files

| File | Purpose | Depends on |
|---|---|---|
| `tests/test_night_runbook_guards.py` | the new seventh property (the check-probe pin) | the shipped parser |
| `docs/planning/p2-rollouts/night-door/runbook.md` | the probe-pass section rewritten code-first | the guard passing |

## Implementation phases

### Phase 1 — the new pin, watched failing (RED)

- **Goal:** a guard property that refuses a sheet missing the check-probe step, a wrong `--run`,
  a dropped condition, or a probe-resume sentence.
- **Files:** `tests/test_night_runbook_guards.py`.
- **Steps:**
  1. RED: `test_the_probe_decision_is_commanded_before_the_night` — asserts, on the **current
     stub** sheet (which has no `check-probe` block): a bash block containing
     `"whetstone check-probe"` exists; it appears before the night block (the `--night` block's
     index); its only flag is `--run` (compared against `build_parser()`'s `check-probe`
     subcommand option strings — add a `_probe_check_flags()` helper mirroring
     `_parser_flags()` at `test_night_runbook_guards.py:137-149`, keyed on the `check-probe`
     choice); the `--run` value is absolute **and** equals the probe block's `--runs`
     root joined with its `--run-id` (`/Users/aliz/dev/at/whetstone/runs/night-probe/probe-001`).
  2. RED: `test_the_decision_sentence_keeps_both_conditions_and_the_exits` — asserts the text
     carries the pre-committed words: "control arm" and "`PASS`" and "every draw", and "non-empty
     seed map" (or the exact sentences), and the exit meanings (a sentence for exit 1: no night
     runs behind it). The current stub fails on all three.
  3. RED: `test_a_probe_is_never_resumed` — asserts the text says a killed probe restarts fresh
     (e.g. "never resumed" for a probe) **and** keeps the killed-night restart verbatim
     ("Restart the **same command, unchanged**"). The stub fails.
  4. RED: the module docstring's "Six properties" line is extended to seven — update it as part
     of the same change so the count matches the tests that exist (the docstring is prose, but
     the house style counts the pins it claims).
- **Validation:** all four fail against the current sheet for their intended reasons.
- **Commit:** `Pin the probe-decision step in the night runbook (TDD, RED)`.

### Phase 2 — the sheet rewrite (GREEN)

- **Goal:** the real sheet satisfies every new pin and keeps every existing one.
- **Files:** `docs/planning/p2-rollouts/night-door/runbook.md`.
- **Steps:**
  1. In **The probe pass** (lines 44-80): after the probe command block, add the third bash
     block:
     ```bash
     uv run --project /Users/aliz/dev/at/whetstone/.claude/worktrees/feat-p2-rollouts \
       whetstone check-probe \
       --run /Users/aliz/dev/at/whetstone/runs/night-probe/probe-001
     ```
     (same worktree the probe block already names — the one-worktree pin holds).
  2. Replace the "Read `runs/night-probe/probe-001/ledger.json` before going further" sentence
     (line 76) with the decision rule as a **command**: exit 0 → proceed to the night; exit 1 →
     a named harness finding (which draw/source, or an empty seed map) and **no night runs
     behind it**; exit 2 → a refusal to fix by retyping, never a verdict about the probe.
  3. Rewrite the pre-committed decision sentence (lines 78-80) keeping **both conditions in the
     pre-committed words** — "control arm `PASS` on every draw" and "a non-empty seed map" —
     and add the probe-restart distinction: *a killed probe is never resumed — restart it fresh
     under a new `--run-id`; a killed **night** still resumes unchanged (below).*
  4. Leave everything else byte-stable: the two `run --night` blocks, the candidate resolution,
     the dev ids, halt conditions, killed-night restart, "Nothing here is published".
- **Validation:** `uv run pytest tests/test_night_runbook_guards.py` — all seven green, including
  every pre-existing pin.
- **Commit:** `Command the probe decision and forbid probe resumes in the night runbook (TDD)`.

### Phase 3 — full suite + lint

- **Steps:**
  1. `uv run ruff check .` and `uv run mypy src/` exit 0.
  2. `uv run pytest` — full suite green (base 1489 passed, 4 skipped).
- **Validation:** all three exit 0.

## Edge cases & error handling

- The check-probe block must use the **same worktree** as the probe block (`feat-p2-rollouts`);
  a second worktree name fails `test_the_runbook_names_exactly_one_worktree_everywhere`.
- The `--run` path must be absolute and exactly the probe block's `--runs` + `--run-id`; a
  relative path or a different run dir fails the new pin.
- The night's restart paragraph is untouched; the probe-restart sentence must **not** bleed into
  it, or the existing killed-night pin breaks.
- No flag value may contain `/reports/` — the `--run` value (`…/runs/night-probe/probe-001`)
  is outside `reports/`, satisfying the existing no-publish pin.

## Agent execution notes

- Phase 1's RED is against the **current** sheet — do not rewrite the sheet first.
- The `check-probe` flag-surface helper must read `build_parser()`'s `check-probe` subcommand
  (mirror `_parser_flags`, `test_night_runbook_guards.py:137-149`); a hard-coded list would
  drift from the shipped parser.
- Keep the six existing prose pins' exact strings intact ("not a halt", "raise `K`",
  "loosen", "Nothing here is published", "zero"/"ceiling").